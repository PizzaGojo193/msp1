name: vpsrun
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v3
      - name: Download Chrome Remote Desktop
        run: |
          Invoke-WebRequest -Uri "https://dl.google.com/edgedl/chrome-remote-desktop/chromeremotedesktophost.msi" -OutFile "chromeremotedesktop.msi"
      - name: Install Chrome Remote Desktop
        run: |
          Start-Process msiexec.exe -Wait -ArgumentList '/I chromeremotedesktop.msi /qn'
      - name: Install Chocolatey
        run: |
          Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://chocolatey.org/install.ps1'))

      - name: Run Chrome Remote Desktop Host
        run: |
          Start-Process "%PROGRAMFILES(X86)%\Google\Chrome Remote Desktop\CurrentVersion\remoting_start_host.exe" -ArgumentList "--code='4/0AeanS0Y6u0pSusFAaqiR6LKmAc1EOC3lLOnkjojp9lEu5t6CU35eFBoLTT9um_w-Mt67_w' --redirect-url='https://remotedesktop.google.com/_/oauthredirect' --name=%COMPUTERNAME%"